# 难度 pro 默认请求探针

- 运行标签：`difficulty-pro-default-request-probe-20260801-a`
- 代码提交：`5408ff6e41e2afe1420ad465aaa4d81ffbfecb85`
- 预期样本：2；成功：0；明确失败：1；失败后未运行：1
- 完整性：不完整
- 失败码：`LLM_HTTP_ERROR`；HTTP 状态：404

Candidate D 固定使用 pro 默认请求、2048 输出上限和每题一次真实请求。人工短题的唯一一次请求得到
HTTP 404，脚本随即熔断，公开题没有发送请求。因此不得启动 83 题正式实验，也不得把本结果用于准确性
比较；这条候选路线在当前登记的服务身份下不可用。

正式标签创建前曾有两次付费前预检拒绝：旧服务身份硬绑定与服务器当前配置不一致。两次都发生在结果目录
和 active 检查点创建前，没有请求或计费，也没有占用正式标签；旧输入按原样改名保留。修正后的正式运行
使用新提交、新环境和新清单，四份私有证据均为 `0600`，最终完成证书逐一绑定检查点、报告和汇总哈希。

本安全摘要不含题面、题解、难度预测、模型原文、服务地址或密钥。私有证据不会进入 Git，其 SHA-256
已写入同名 JSON 摘要。
