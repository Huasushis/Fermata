# 难度基线不完整运行登记

这里只登记不含题面、模型回答、密钥或服务地址的安全摘要。不完整运行永远不能作为修改前或修改后的准确性报告。

## `public83-before-20260801-v1`

- 代码提交：`ca329aec992174a0267a2423d781f560a54f43b9`
- 公开清单 expected：83
- 退出时检查点：成功 0、明确失败 1、崩溃遗留 active 3、尚未启动 79
- 固定失败：`LLM_HTTP_ERROR`，HTTP 状态 404
- 随后异常：非成功响应正文清理触发 Node `ERR_INVALID_STATE`，进程未能写出报告三件套
- 报告资格：`complete=false`、`eligible=false`；不得续跑或用于准确性比较
- 宿主核验：记录 PID 已不存在，且没有工作目录属于本项目、脚本为 `eval-difficulty.ts` 的实验进程
- 遗留锁：核验后以硬链接保留私有崩溃证据，再移除固定锁；检查点和证据均为 `0600`
- 私有检查点 SHA-256：`d96f065ac1634d432573e35da853d3db6a4840aecfef87f58af857c3d8603fd6`
- 私有锁证据 SHA-256：`4a9be5a57c46ee013269739226029a4bd4721250a047693496ae49874c70ac03`

修复传输层清理和失败链恢复语义后，必须使用全新标签从零运行修改前基线。旧检查点原样保留，不补跑、不改写，也不生成一份看似完整的报告。

## `public83-before-20260801-v2`

- 代码提交：`5df5717fabf412d47d36b90593e5dfd9e81f14fe`
- 修正项：模型基础路径按公开配置约定补齐 `/v1`；启动前一条不含题库内容的合成请求已完整等到 HTTP EOF
- 公开清单 expected：83
- 报告结果：成功 3、模型响应超过 4 MiB 上限 4、未启动或未完成 76、调度收束失败码 1
- 完整性：`complete=false`；该报告只能作为失败证据，不能作为修改前准确性基线
- 收束行为：首个失败后停止发起新样本，等待其余 3 个在途请求自然结束；没有主动取消、499 或自动重试
- 证据：安全 summary、Markdown 和绑定 raw/summary/Markdown 哈希的 completion marker 已保留；raw 只留在 Git 忽略区

四个同时在途的 difficulty 响应都超过固定 4 MiB 安全上限，说明短 JSON 请求缺少服务端输出 token 上限。后续只能先增加不会影响正常短回答的有界输出保护并保留本节证据，再使用全新标签从零运行；不得提高 4 MiB 客户端读取上限或续跑本链。

## `public83-before-20260801-v3`

- 代码提交：`cddb1bf7f299480029a60a10ccdb964911f0435a`
- 修正项：difficulty 请求增加 2048 token 的服务端输出上限；启动前使用人工合成题目完成全链路探针并等到 HTTP EOF
- 公开清单 expected：83，清单指纹核验通过
- 报告结果：成功 2、模型响应格式无效 4、停止调度 1、未启动或未完成 77
- 完整性：`complete=false`；该报告只能作为失败证据，不能作为修改前准确性基线
- 收束行为：首个失败后停止发起新样本并等待其余在途请求自然结束；安全报告中没有 499 或取消固定码
- 证据：安全 summary、Markdown 和绑定 raw/summary/Markdown 哈希的 completion marker 已保留；raw 为 `0600` 且只留在 Git 忽略区

服务端输出上限避免了无界增长，但四个在途样本没有返回满足既有短 JSON 契约的完整结果。下一步必须先用不含题库内容的探针区分“达到输出上限”与其他格式错误，再决定标准兼容的短输出方案；不得放宽 4 MiB 客户端上限、续跑本链或把这两个成功样本当作准确性数据。

## `public83-after-rubric-v1-20260801`

- 代码提交：`16b2449ed3d635610ef7b77db74049b22bf967fe`
- 候选行为改动：补充完整的 800–3500 难度量尺、必要瓶颈判定和占位题解规则；另同步更新实验版本标识、文档和测试。模型、锚点、思考模式、输出上限与等待规则不变
- 公开清单 expected：83，清单指纹核验通过
- 报告结果：成功 19、模型响应格式无效 1、停止调度 1、未启动 63
- 完整性：`complete=false`；该报告不能用于修改前后准确性比较，也不能引用其中的临时 MAE 或命中率
- 收束行为：首个失败后停止发起新样本并等待其余在途请求自然结束；安全报告中没有 499 或取消固定码
- 证据：安全 summary、Markdown 和绑定 raw/summary/Markdown 哈希的 completion marker 已保留；raw 为 `0600` 且只留在 Git 忽略区

当前客户端会把达到输出上限、内容过滤等非 `stop` 完成原因统一折叠为 `LLM_RESPONSE_FORMAT_INVALID`，因此本报告不能证明具体原因。下一步先把已知完成原因映射为不含服务商原文的固定错误码，再用新标签从零运行；不得续跑本链、猜测原始回答或仅凭提高上限掩盖其他格式错误。

## `public83-after-rubric-v1-fixedcodes-20260801`

- 代码提交：`0267dcec9fa6e89b59e9070ea5773b063c0b5206`
- 诊断修正：把输出长度限制和内容过滤分别映射为不含服务商原文的固定错误码；提示词、模型、锚点、思考模式、2048 token 输出上限与等待规则均不变
- 启动前探针：对上一链失败的一个公开样本重新运行并等到真实 HTTP EOF，结果成功；没有据此提高输出上限
- 公开清单 expected：83，清单指纹核验通过
- 报告结果：成功 83、失败 0、`complete=true`；没有 499、取消、缺失、跳过或残留在途样本
- 准确性：MAE 289.2、±200 命中率 55.4%（46/83）；相对修改前 MAE 改善 13.3，但命中率下降 1.2 个百分点
- 分档结论：低档 MAE 177.8、中档 204.2、高档 387.8；仍未达到 MAE ≤ 200、命中率 ≥ 75% 的既定门槛
- 证据：安全 summary、Markdown 和绑定 raw/summary/Markdown 哈希的 completion marker 已保留；raw 为 `0600` 且只留在 Git 忽略区

该候选只能证明完整量尺略微降低了总体绝对误差，不能证明准确性通过，也不能作为已标定配置部署。下一轮必须使用新的实验版本、代码提交和标签，从零生成完整报告；不得覆盖或续跑本链。
